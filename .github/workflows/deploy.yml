on:
  push:
    branches:
      - main
      - staging

name: 🚀 Deploy website on push

jobs:
  deploy-main:
    name: 🎉 Deploy to main
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Deploy to main
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: ftp.atomtroxell.com
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASSWORD }}
          dry-run: true
          local-dir: ./dist # Adjust to your 11ty build directory
          remote-dir: /home/atomtrox/helloatomtech # Remote directory for main branch

  deploy-staging:
    name: 🎉 Deploy to staging
    if: github.ref == 'refs/heads/staging'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Deploy to staging
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: ftp.atomtroxell.com
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASSWORD }}
          dry-run: true
          local-dir: ./dist # Adjust to your 11ty build directory
          remote-dir: /home/atomtrox/staging.helloatomtech.com # Remote directory for staging branch
